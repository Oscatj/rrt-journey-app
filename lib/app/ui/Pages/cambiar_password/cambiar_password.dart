import 'package:flutter/material.dart';

class CambiarPasword extends StatelessWidget {
  const CambiarPasword({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: ListView(),
    );
  }
}